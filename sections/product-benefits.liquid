{%- assign meta_benefits = product.metafields.custom.benefits.value -%}
<section class="product-benefits full-width">
  <div class="product-benefits__container">
    <div class="product-benefits__sidebar" data-animate="fade-up">
      <div class="product-benefits__sidebar-header">
        {% if section.settings.tag_text != blank %}
          <span class="product-benefits__tag">{{ section.settings.tag_text }}</span>
        {% endif %}

        {% if section.settings.heading != blank %}
          <h2 class="product-benefits__heading">{{ section.settings.heading }}</h2>
        {% endif %}
      </div>

      <ul class="product-benefits__list" role="list">
        {%- if meta_benefits and meta_benefits.size > 0 -%}
          {%- for benefit in meta_benefits -%}
            <li class="product-benefits__item">
              <div class="product-benefits__item-text">
                {%- if benefit.title != blank -%}
                  <h3 class="product-benefits__item-title">{{ benefit.title }}</h3>
                {%- endif -%}
                {%- if benefit.description != blank -%}
                  <p class="product-benefits__item-desc">{{ benefit.description }}</p>
                {%- endif -%}
              </div>
            </li>
          {%- endfor -%}
        {%- else -%}
          {% for block in section.blocks %}
            {% if block.type == 'benefit' %}
              <li class="product-benefits__item" {{ block.shopify_attributes }}>
                <div class="product-benefits__icon">
                  {% if block.settings.icon %}
                    {{ block.settings.icon | image_url: width: 48 | image_tag: loading: 'lazy' }}
                  {% elsif block.settings.icon_name != blank %}
                    {{ block.settings.icon_name | append: '.svg' | inline_asset_content }}
                  {% else %}
                    {{ 'icon-benefit-quality.svg' | inline_asset_content }}
                  {% endif %}
                </div>
                <div class="product-benefits__item-text">
                  {% if block.settings.title != blank %}
                    <h3 class="product-benefits__item-title">{{ block.settings.title }}</h3>
                  {% endif %}
                  {% if block.settings.description != blank %}
                    <p class="product-benefits__item-desc">{{ block.settings.description }}</p>
                  {% endif %}
                </div>
              </li>
            {% endif %}
          {% endfor %}
        {%- endif -%}
      </ul>
    </div>

    <div class="product-benefits__details" data-animate="fade-up" data-animate-stagger>
      {%- if meta_benefits and meta_benefits.size > 0 -%}
        {%- for benefit in meta_benefits -%}
          <div class="product-benefits__detail">
            <div class="product-benefits__detail-text">
              {%- if benefit.title != blank -%}
                <h3 class="product-benefits__detail-title">{{ benefit.title }}</h3>
              {%- endif -%}
              {%- if benefit.description != blank -%}
                <p class="product-benefits__detail-description">{{ benefit.description }}</p>
              {%- endif -%}
            </div>

            <div class="product-benefits__detail-image">
              {%- if benefit.image -%}
                <img src="{{ benefit.image }}" loading="lazy" alt="{{ benefit.title | escape }}">
              {%- else -%}
                {% render 'placeholder-image' %}
              {%- endif -%}
            </div>
          </div>
        {%- endfor -%}
      {%- else -%}
        {% for block in section.blocks %}
          {% if block.type == 'benefit' %}
            <div class="product-benefits__detail" {{ block.shopify_attributes }}>
              <div class="product-benefits__detail-text">
                {% if block.settings.title != blank %}
                  <h3 class="product-benefits__detail-title">{{ block.settings.title }}</h3>
                {% endif %}

                {% if block.settings.description != blank %}
                  <p class="product-benefits__detail-description">{{ block.settings.description }}</p>
                {% endif %}
              </div>

              <div class="product-benefits__detail-image">
                {% if block.settings.image %}
                  {{ block.settings.image | image_url: width: 1200 | image_tag: loading: 'lazy' }}
                {% else %}
                  {% render 'placeholder-image' %}
                {% endif %}
              </div>
            </div>
          {% endif %}
        {% endfor %}
      {%- endif -%}
    </div>
  </div>
</section>

{% stylesheet %}
  .product-benefits {
    background-color: var(--theme-surface);
    padding: var(--space-13) 0;
  }

  .product-benefits__container {
    display: flex;
    gap: var(--space-12);
    max-width: var(--container-max, 80rem);
    margin: 0 auto;
    padding: 0 var(--container-padding, 16px);
    align-items: flex-start;
  }

  /* ── Sidebar (dark green card) ── */
  .product-benefits__sidebar {
    flex: 0 0 42%;
    position: sticky;
    inset-block-start: var(--space-6);
    background-color: var(--theme-primary);
    border-radius: var(--radius-card);
    padding: var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .product-benefits__sidebar-header {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .product-benefits__tag {
    display: inline-block;
    align-self: flex-start;
    background-color: var(--color-on-primary-subtle);
    color: var(--theme-text-on-primary);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-medium);
    text-transform: uppercase;
    letter-spacing: var(--letter-spacing-wider);
    padding: var(--space-1) var(--space-3);
    border-radius: var(--theme-radius-badge);
    line-height: 18px;
  }

  .product-benefits__heading {
    font-size: 3.25rem;
    line-height: 4rem;
    font-weight: var(--font-weight-regular);
    color: var(--theme-text-on-primary);
    letter-spacing: var(--letter-spacing-tighter);
    margin: 0;
  }

  /* ── Sidebar benefit list ── */
  .product-benefits__list {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .product-benefits__item {
    display: flex;
    flex-direction: row;
    align-items: flex-start;
    gap: var(--space-3);
    padding: var(--space-3);
  }

  .product-benefits__item-text {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    flex: 1;
    min-width: 0;
  }

  .product-benefits__icon {
    width: 24px;
    height: 24px;
    flex-shrink: 0;
    color: var(--theme-text-on-primary);
  }

  .product-benefits__icon img,
  .product-benefits__icon svg {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: contain;
  }

  .product-benefits__item-title {
    font-size: 1.25rem;
    font-weight: var(--font-weight-regular);
    color: var(--theme-text-on-primary);
    line-height: 1.75rem;
    margin: 0;
  }

  .product-benefits__item-desc {
    font-size: 1rem;
    font-weight: var(--font-weight-regular);
    color: var(--color-text-on-dark-muted);
    line-height: 1.5rem;
    margin: 0;
  }

  /* ── Detail panels (right column) ── */
  .product-benefits__details {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .product-benefits__detail {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .product-benefits__detail-text {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    padding: 0 var(--space-2);
  }

  .product-benefits__detail-title {
    font-size: var(--font-size-3xl);
    line-height: 44px;
    font-weight: var(--font-weight-regular);
    letter-spacing: var(--letter-spacing-tighter);
    color: var(--theme-primary);
    margin: 0;
  }

  .product-benefits__detail-description {
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-regular);
    line-height: 24px;
    color: var(--theme-text-secondary);
    margin: 0;
  }

  .product-benefits__detail-image {
    width: 100%;
    height: 381px;
    border-radius: var(--radius-card);
    overflow: hidden;
    position: relative;
    background-color: var(--color-surface-card);
  }

  .product-benefits__detail-image img,
  .product-benefits__detail-image svg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .product-benefits__placeholder {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* ── Mobile ── */
  @media screen and (max-width: 749px) {
    .product-benefits {
      padding: var(--space-6) 0;
    }

    .product-benefits__container {
      flex-direction: column;
      gap: var(--space-8);
    }

    .product-benefits__sidebar {
      flex: none;
      width: 100%;
      position: static;
      padding: var(--space-6);
      gap: var(--space-6);
      box-sizing: border-box;
    }

    .product-benefits__details {
      width: 100%;
    }

    .product-benefits__heading {
      font-size: var(--font-size-3xl);
      line-height: var(--line-height-tight);
    }

    .product-benefits__list {
      display: none;
    }

    .product-benefits__detail-title {
      font-size: var(--font-size-2xl);
      line-height: var(--line-height-snug);
    }

    .product-benefits__detail-image {
      height: auto;
      aspect-ratio: 564 / 381;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:sections.product_benefits.name",
  "settings": [
    {
      "type": "text",
      "id": "tag_text",
      "label": "t:sections.product_benefits.settings.tag_text",
      "default": "BENEFITS"
    },
    {
      "type": "textarea",
      "id": "heading",
      "label": "t:labels.heading",
      "default": "Thoughtfully designed product"
    }
  ],
  "blocks": [
    {
      "type": "benefit",
      "name": "t:sections.product_benefits.blocks.benefit.name",
      "settings": [
        {
          "type": "image_picker",
          "id": "icon",
          "label": "t:labels.icon"
        },
        {
          "type": "text",
          "id": "icon_name",
          "label": "Icon SVG name",
          "info": "Asset filename without .svg (e.g. icon-benefit-quality). Used when no image icon is set."
        },
        {
          "type": "text",
          "id": "title",
          "label": "t:labels.title"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "t:labels.description"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "t:labels.image"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.product_benefits.name",
      "blocks": [
        {
          "type": "benefit",
          "settings": {
            "icon_name": "icon-benefit-quality",
            "title": "Quality-focused design",
            "description": "Built with attention to detail and carefully selected components."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "icon_name": "icon-benefit-expert",
            "title": "Developed with expert input",
            "description": "Created with guidance from experienced designers and product specialists."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "icon_name": "icon-benefit-innovation",
            "title": "Innovative construction",
            "description": "Engineered with modern materials and techniques for reliable performance."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "icon_name": "icon-benefit-convenience",
            "title": "Convenient for everyday use",
            "description": "Designed to fit easily into your daily routine without extra steps."
          }
        }
      ]
    }
  ],
  "disabled_on": {
    "groups": ["header", "footer"]
  }
}
{% endschema %}
